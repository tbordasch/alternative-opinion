/** @type {import('next').NextConfig} */
const nextConfig = {
  // Für Vercel: Keine output: 'export' nötig
  // Für Strato statischen Export: Entferne Kommentar unten
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
